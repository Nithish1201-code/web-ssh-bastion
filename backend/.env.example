# SSH Configuration
SSH_MODE=mock                           # Options: 'mock' or 'real'
SSH_USER=ubuntu
SSH_KEY_PATH=/keys/id_ed25519
SSH_PORT=22

# Server Configuration
PORT=3000
HOST=localhost

# Target Containers / VMs (JSON array as string)
# This is a placeholder; you'll add real targets later
SSH_TARGETS=[]
